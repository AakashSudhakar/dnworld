<a href="/">Back to Home</a>
<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    Show Status!
    <h1>{{> statuses.title }}</h1>
    <h5>{{> statuses.city }}</h5>
    <h4>{{> statuses.country }}</h4>
    <p>{{> statuses.description }}</p>
  </div>
</div>
